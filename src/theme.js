import { createTheme } from "@mui/material/styles";

const muiFiveTheme = createTheme({
  typography: {
    fontFamily: "Manrope",
    h1: {
      fontSize: "64px",
    },
    h2: {
      fontSize: "58px",
    },
    h3: {
      fontSize: "48px",
    },
    h4: {
      fontSize: "40px",
    },
    h5: {
      fontSize: "32px",
    },
    h6: {
      fontSize: "20px",
    },
    title: {
      fontSize: "32px",
    },
  },
});

export default muiFiveTheme;
